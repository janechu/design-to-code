import { html } from "@microsoft/fast-element";
import { SingleLineControl } from "./frame.single-line.js";

/**
 * The template for the form component.
 * @public
 */
export const template = html<SingleLineControl>``;
